# User Profile

- Mobile developer: apps (React Native / mobile web), HTML/CSS/JS, C++.
- This project (`appmxh`) is a mobile web app: `website/` (HTML/JS/CSS), `database/` (SQL schemas).
- Language: user writes in Vietnamese — reply in Vietnamese.

# Working preferences

- When analyzing or editing, proactively read MULTIPLE related files together (batch tool calls in parallel) instead of one at a time: e.g. pair HTML with its JS/CSS, or code with its SQL schema.
- Follow existing code style in each file; no unnecessary comments.
- Prefer checking related schemas in `database/` before changing data-layer code.
